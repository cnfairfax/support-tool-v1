import React, { Component } from "react";
 
class Home extends Component {
  render() {
    return (
      <div>
        <h2>Email Unblock Tool</h2>
        <p>Use the links on the sidebar to unblock email addresses, check customer email lists against the unblock database, and pull past email unblock reports for a given account.</p>
      </div>
    );
  }
}
 
export default Home;